Update the AspenOak menu.

Ask me what needs to change — options are:
1. Add a new menu item (ask: section, name, description, price, any tags like "Seasonal" or "New!")
2. Edit an existing item (ask: which item, what changes)
3. Remove an item (ask: which item)
4. Update a section header or badge
5. Add/remove/edit a specials callout
6. Update the seasonal section (Easter menu or similar)

Once I confirm the change, edit `site/menu.html` directly. After editing, remind me to:
- `git add site/menu.html`
- `git commit -m "Update menu: [brief description]"`
- `git push origin main`
- Verify the change went live on Netlify within 1 minute
