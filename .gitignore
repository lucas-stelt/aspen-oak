# Environment variables — never commit these
.env
.env.local
.env.production

# Netlify
.netlify/

# Node
node_modules/

# iPhone photo originals — convert to web JPEG via automation/convert_heic.py, don't commit
*.heic
*.HEIC
